%header
  .container
    %ul
      %li#logo
      - if logged_in?
        %li.rightmost= link_to 'Logout', logout_path, method: :delete
      - else
        %li.rightmost= link_to 'Login', login_path
      .clear